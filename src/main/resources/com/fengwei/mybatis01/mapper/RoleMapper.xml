<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fengwei.mybatis01.mapper.RoleMapper">
    <select id="selectRoleById3" resultType="SysRole">
        select * from sys_role where id=#{id}
    </select>
</mapper>